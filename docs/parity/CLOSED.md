# Closed parity items

Cold archive for verified (`OK`) and accepted known-difference (`DIFF`) rows. Search it only when a closed feature is relevant.

| Area | Feature | Legacy → current | St | Evidence |
|---|---|---|---|---|
