# UX parity matrix (moved)

The parity tracker is sharded under `docs/parity/` to keep task context small. Start at [`parity/INDEX.md`](parity/INDEX.md) and search for the relevant feature; do not preload every shard.
